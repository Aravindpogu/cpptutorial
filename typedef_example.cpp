#include<iostream>

using namespace std;

typedef unsigned short StudentId;
enum Color {
	RED=2, YELLOW, GREEN=7 // Here YELLOW will be RED+1. ie, 3
};
//void process(StudentId id);
//void processColor(Color color);


int main(){

	StudentId studentOne = 1001;
	Color myColor = YELLOW;
	//processColor(myColor);
}
	
